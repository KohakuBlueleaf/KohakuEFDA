[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "kohakulayout-rs"
version = "0.1.0"
description = "The native twin of KohakuLayout"
requires-python = ">=3.13"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "kohakulayout_rs"
