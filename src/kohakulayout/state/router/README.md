# kohakulayout.state.router

The router slot. The world calls `route(world, net)` inside the placing transaction;
the router writes wires and units through the world and refuses at stage `route`.

| file | what |
|---|---|
| `protocol.py` | `Router` protocol (`route`, `unroute`, `cost`), `Terminal` (with the ports a pin may still take as `options`, and `bound` when it has one), `Costs`, `terminals(world, net)` (the placed pins; a refusal without a placed source or sink), `refuse`, the registry (`register`, `make_router`, `ROUTERS`) |
| `reservations.py` | `walls(world, carrier)` from regions the pack closes to the carrier; `unit_walls(world, unit)` from regions the pack closes to a unit (`Boundaries.unit_region`), so a crossing unit never stands there; `reservation_price`: corridor for the own carrier, wall for any other |
| `pathfinder.py` | `Search` (one search's view; `own`: the net's own standing-lane cells with their axis, crossed with the carrier's own unit at either end of a path or on the way, `own_crossing`), `entry` (the cost of a cell: free, shared, crossing (a bending wire only on a path's start cell), ripped, a field emitter displaced at `Costs.displace`, or closed), `find` (sources opened in a given `order`, neighbours N, E, S, W, no U-turn, the span to the targets' bounding box as the estimate, `end_on_crossing` and `float_scale` as options), `query_of` (one search's own rules for the native twin: its net and carrier, costs, flags, and the cells shut, held or charged), `register_tables` (what every search reads and no search owns, handed to the native grid once per netlist, physics and set of reservations: the nets with their carriers, each carrier pair's sharing and crossing rule, the crossing units' shapes, the reservations' carriers; a unit's facts reach the kernel as it is placed, `Kernel.note_unit`), `find` (A* from a cell set to a cell set with turn costs, bounded by `max_steps`; on a native kernel the twin's search answers first); another net's open attach cells are shut and its routed attach cells are never ripped, and a routed attach cell counts as straight-through from its port (a bridge in front of the port); a search gives up past `detour` times the span across plus `slack` when the costs set them; walls and the carrier's unit walls reach the native grid once per key (`register_walls`), and a crossing that needs a unit is refused on a unit wall; a path may start on a cell another wire runs straight through, leaving across it, and end on one, arriving across it, the crossing recorded on the terminal cell (`entry(..., terminal=True)` judges a start by what holds it alone) |
| `trees.py` | `Plan`, `Seed`/`seed_of`, `TreePolicy` (which placed pins a grow reaches now, `pending`; which placed source it grows from, `root`; the lanes of a net in laying order, `lanes`, their rank across nets, `rank`, the standing lanes a stuck lane may take up, `blockers`, and whether a lane may be one cell, `single_cell_lane`, for the lane router; which sinks the trunk may end at (`trunk`, given the world and the root); which tree cells a join or a branch may leave from, given the world and the net; the default keeps every option), `held_by_others` (the nets a terminal cell's search must rip, a wire it may cross there not among them), a pin whose attach cell the seeded tree already crosses seated there with its junction (`may_seat`), a port serving one pin of a cell (`open_for`: a seed end, a tree cell or a seat a sibling pin took is closed to the next, and the root settles where a first pin meets it), a seed's join segments read tree-ward, `feeds` (a source's port or the outside sends flow in), `may_join` (the pack allows the junction unit in the cell's regions, no unit on the cell, its occluded layers free), `grow` (a tree with arrivals and departures per cell: a trunk from the root to the nearest sink, then sources join with a merge wherever flow already arrives, then sinks leave with a split wherever flow already leaves; a path never runs through a terminal it is not reaching; a junction unit never on a crossing or another junction); `held_by_others` (a terminal cell another wire holds is ripped back or refused, never shared; one under a footprint or another owner's unit cannot be taken); a pin with several ports is one terminal group, the plan records the port the path reached (`Plan.ports`) |
| `units.py` | crossing units, junction units and repeaters (a run counted between the unit cells on a segment; a stretch over the limit cut on the last straight cell within the limit, else its second cell) placed through `world.place_unit`; a field emitter in a unit's way is removed (`emitter_at`) and every placed cell left short is covered again (the chain's `recover`); a reused crossing unit that went with a ripped net is placed again (`unit_at`) |
| `lanes.py` | `LaneRouter`: the default router laying a net as a bundle of lanes (`lay`: each lane from its source pin's own lanes' straight cells, or its open attach cells, to its sink pin's, port to port when neither has one; a split where a lane leaves another, a merge where it ends on one; a bound pin's only attach cell reserved for it; a lane crossing another of its net on a straight free cell through `Search.own`), `route_all` (the ready lanes of the nets a placement touches, in the policy's `rank` across nets, one at a time), a worklist that takes up the policy's `blockers` once for a lane with no cell to start from or end on and lays them again after it, `single_cell_lane` asked before a one-cell lane, searches that never end on a crossing and accumulate costs as single floats when `float_scale` is set, `whole_lanes`/`lane_pins`/`attach_pins` (the standing segments read back to their pins, a cut lane dropped and laid again), `standing_crossings` (a seed's crossing kept only while the kept segments still make it: another net's wire on the cell, or two kept segments of the net over it), `standing_lanes`, `crossable_own`; a pin's sub-plan carries the whole plan's segments in laying order (`Plan.standing`) for the policy |
| `default.py` | `DefaultRouter`: `order` (the nets a placement touches, the widest span first; an override point), `plan` (the growth under the router's `policy`, a `TreePolicy`; an override point), negotiated congestion in passes (`passes`, `growth`, `max_rips`): a net routes at the present cost, ripping what stands in its way, the displaced re-route in the same pass without displacing anyone, every pass raises the present cost until nothing stays displaced or the route rolls back; `commit` removes the emitters the plan displaces, places crossing units, junctions and repeaters, covers again after a displacement, and rips a wire under a unit |
| `native_route.py` | the world's records mirrored in the twin (`Mirror`, `statics`, `sync_doc`, the unchanged sync on the world's revision), the native routing pass (`native_route_all`), the native placement attempt (`native_attempt`) and admission check (`native_admits`); each answers None for Python to decide |

## Dependencies

`kohakulayout.errors`, `kohakulayout.ir`, `kohakulayout.physics`, `kohakulayout.state.kernel`.
